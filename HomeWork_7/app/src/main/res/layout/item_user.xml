<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="user"
            type="org.kimp.tfs.hw7.data.api.Profile" />
        
        <import type="org.kimp.tfs.hw7.utils.InitialsKt" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/base_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <org.kimp.tfs.hw7.presentation.base.AvatarView
            android:id="@+id/avatar_view"
            android:layout_width="@dimen/user_item_avatar_view_size"
            android:layout_height="@dimen/user_item_avatar_view_size"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:badgeRadius="6dp"
            app:hasBadge="@{user.active}"
            app:initials="@{InitialsKt.extractInitials(user.fullName)}"
            tools:src="@drawable/me"
            tools:hasBadge="true"/>

        <LinearLayout
            android:layout_marginStart="@dimen/user_item_avatar_view_right_margin"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/avatar_view"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:orientation="vertical">

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/full_name_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{user.fullName}"
                android:textStyle="bold"
                android:textSize="@dimen/user_item_user_name_text_size"
                android:maxLines="1"
                tools:text="Mikhedov Konstantin"/>

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/email_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{user.email}"
                android:textSize="@dimen/user_item_user_email_text_size"
                android:maxLines="1"
                tools:text="mihedovkos@gmail.com" />
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
